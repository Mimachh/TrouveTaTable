import React from "react";
import { Button } from "./button";

const RegisterButton = () => {
    return (
        <Button variant={"default"} className="px-5 rounded-full text-muted/90">
            Inscription
        </Button>
    );
};

export default RegisterButton;
